"""Strings, paths and such."""

import os as _os

SRC_PATH = r".\src"
GUI_PATH = _os.path.join(SRC_PATH, "gui")
ICONS_PATH = _os.path.join(GUI_PATH, "icons")
PLACEHOLDERS_PATH = _os.path.join(GUI_PATH, "placeholders")

APP_TITLE = "QMK Thor"
